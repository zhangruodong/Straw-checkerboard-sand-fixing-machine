# 安全须知与免责声明

> ⚠️ **本仓库是控制思路的参考实现，不是可以直接上机运行的产品固件。**
> 它驱动机器人**运动部件**（履带行走 + 推杆执行机构），接线错误或参数未标定可能导致设备损坏、人身伤害或财产损失。投产前必须由具备相应能力的人员完成电气检查、参数标定与空载验证。

## 一、这是参考实现

- 代码按**一套特定的机械/电气配置**编写。换电机、换推杆、换减速比、换轮径，都必须**重新标定**并重新验证，不能直接套用。
- `Hardware/system.h` 里的 `GRID_STEPS`、`TURN_STEPS`、`ROD_DOWN_MS`、`ROD_UP_MS`、`PRESS_MS` 等是**占位值**，取决于你的机械安装，必须实测填入（标定方法见 [README.md](README.md) 的「标定」一节）。
- `TRACK_FWD`/`TRACK_BWD` 方向、限位开关常开/常闭，同样取决于接线，必须实测确认。

## 二、上电前必须确认

1. **接线逐项核对** [README.md](README.md) 的接线表，特别是 `PB3`/`PB4`（关闭 JTAG 后才释放为推杆继电器，接错会与调试口冲突）。
2. **确认推杆双继电器不会同时导通**——伸出与收回继电器接同一推杆时，程序有互锁，但接线本身必须核对无误，否则上电即短路。
3. **确认限位开关方向正确**：压到应为低电平（内部上拉）。接反会导致「已到位」判断取反，推杆会一直顶。
4. **确认有独立的物理断电手段**（见第三节第 1 条）。
5. **先空载、后带载**：脱开机械负载，确认电机转向、推杆伸缩方向、限位触发都对，再装上负载。
6. **先单步、后自动**：先用单条指令（`P100`、`A`/`a` 等）逐项验证，确认无误再跑 `G` 全自动流程。

## 三、本项目的已知危险点

1. **没有物理急停按钮。** 代码里急停只有串口 `T` 指令一条路——**串口线掉了、上位机死机了，你就没有别的办法让车停下来**。强烈建议在硬件上串一个**物理急停开关**（直接切断电机/推杆动力回路，而不是只通知 MCU）。这是投产前的**必要**改动。
2. **自动铺草期间没有通信超时保护。** 通信超时判定明确排除了自动状态（`Hardware/system.c` 中 `state != STATE_AUTO` 条件）——也就是说，一旦用 `G` 启动自动流程，**即使上位机失联，机器也会继续跑完整个循环**，不会自动停。人员必须全程在旁监护。
3. **推杆互锁只存在于软件层。** 继电器触点粘连、程序跑飞、或 GPIO 上电瞬态，都可能让伸/收同时导通。硬件层面应有互锁或保险。
4. **超时兜底值不可靠。** 推杆到位判断依赖限位开关，限位失效时靠 `ROD_DOWN_MS`/`ROD_UP_MS` 超时兜底，而这些超时值是基于实测定时长的**估计**，不是安全保证。
5. **标定指令会让车走很远。** `P`/`N`/`L`/`R` 步数上限为 30000，而脉冲频率约为 **1 kHz（1 步 ≈ 1 ms）**，即单条指令最多可让车**连续行走约 30 秒**。标定时人不要在车的行进路线上，且确保随时能断电。

## 四、代码中已有的保护措施及其局限

| 措施 | 实现 | 局限 |
|---|---|---|
| 独立看门狗 | IWDG，约 2 秒超时（LSI 40 kHz / 64，重装 1250） | 只能应对程序卡死；看门狗复位后外设回默认态，是"停"而不是"安全停" |
| 通信超时 | 120 秒无指令自动停止（`COMM_TIMEOUT_MS = 120000`） | **排除自动状态**，见第三节第 2 条 |
| 推杆互锁 | 软件保证伸/收不同时输出 | 软件层，不防硬件故障 |
| 限位开关 | 前/后/上升/下降到位检测 | 传感器/接线失效时退化为超时判断 |
| 调试冻结 | `DBGMCU_IWDG_STOP`，调试时停看门狗 | 仅调试期有效 |

## 五、免责声明

- 本项目以**「按现状」（AS IS）**提供，**不附带任何形式的担保**，无论是明示的、默示的，还是法定的，包括但不限于对**适销性、特定用途适用性、不侵权**的默示担保。
- 在**任何情况下**，作者或版权持有人均**不对任何索赔、损害或其他责任**负责，无论是在合同诉讼、侵权行为还是其他情形下，由本软件、本软件的使用或其他处理方式引起、产生或与之相关的。
- **使用者须自行承担全部风险**，包括但不限于设备损坏、人身伤害、财产损失及第三方损失。
- 本文件是**工程安全提示**，不是法律意见，也**不构成对任何特定司法管辖区下责任的排除或限制**。

---

引用的第三方代码许可条款见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)；本项目自有代码的许可范围见 [LICENSE](LICENSE) 与 [README.md](README.md) 的「许可」一节。
